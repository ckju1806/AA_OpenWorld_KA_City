# config/

Zweck: projektweite Konfigurationshinweise außerhalb von Godot (z. B. Toolchain-Versionen).
Godot-eigene Konfiguration liegt in `project.godot` und `export_presets.cfg` im Repo-Root.
Enthält **keine** Zugangsdaten. Siehe `toolchain.md`.
